using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.Json;
using System.Threading.Tasks;

namespace DragonslayerUppgiftLudvig;
internal class SaveGame
{
    //custom class
    //static void WriteJson()
    //{
    //    Book book = new Book("It", 400);
    //    string json = JsonSerializer.Serialize(book); //{ "Title": "It", "Pages": "400"}

    //    var filePath = GetFilePath(JsonFileName);
    //    File.WriteAllText(filePath, json);
    //}

    //static void ReadJson()
    //{
    //    string filePath = GetFilePath(JsonFileName);
    //    string json = File.ReadAllText(filePath);

    //    Book book = JsonSerializer.Deserialize<Book>(json)!;

    //    Console.WriteLine(json);
    //    Console.WriteLine($"{book.Title} {book.Pages}");
    //}

    public static void Saving()
    {
        Console.WriteLine("In progress...");
    }
}
